
# This is a basic workflow to showcase the use of Reusable Workflows call

name: Reusable Workflow user 4

on:
  workflow_dispatch:
jobs:
  call-workflow-1-in-local-repo:
    uses:shafivullashaik/cic-cd-git-hub-actions/.github/workflows/blank.yml
